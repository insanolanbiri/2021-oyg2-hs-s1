#include <stdio.h>
int main()
{
    /**
     * for(değişken;şart;değişim) {olay;}
     * değişken -> sürekli: {şart -> olay -> değişim}
    */

    int i=0;
    for (;;) {printf("i=%d",i);}
    return 0;
}
